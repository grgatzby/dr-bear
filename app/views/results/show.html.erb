<h1>Results page</h1>


<div>
<p>Category name:<%= @category.name %></p>
<p>Category description:<%= @category.description %></p>
</div>
<div>
<% @nutrient.each do |nutrient| %>
<% nutri = Nutrient.find(nutrient) %>
<p>Nutrient name:<%= nutri.name %></p>
<p>Nutrient description:<%= nutri.description %></p>
<% end %>


</div>
<div class= "fa-solid fa-bars"></div>
<%= image_tag "", class: "fa-solid fa-bars", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>

<%= link_to 'See our recommended options', basket_path(@result.id), data: {"turbo_method": :patch} %>
