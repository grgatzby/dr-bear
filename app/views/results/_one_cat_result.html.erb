
<% nutrients.each do |nutrient| %>
  <% nutri = Nutrient.find(nutrient) %>

  <% unsplash_url = "https://source.unsplash.com/200x200/?#{nutri.name}-supplement" %>

  <div class="nutrients-content-container" onclick="window.open('https://en.wikipedia.org/wiki/<%= nutri.name %>');">
    <div class="nutrient-image-container" style="background-image: url('<%= unsplash_url %>');"></div>
    <div class="nutrient-content-text" data-controller="popover">
      <div class="nutrient-name-container">
        <h2 class="nutrient-card-title">
          <%= nutri.name %>
        </h2>
      </div>
      <div class="nutrient-description-container">
        <p class="nutrient-card-body seventyone-chars">
          <!-- <%= nutri.description %> -->
          Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod
        </p>
        <a href="#" class="button" data-action="mouseenter->popover#show mouseleave->popover#hide">Learn More</a>
      <template data-popover-target="content">
        <div data-popover-target="card" class="max-w-sm rounded shadow-lg bg-white absolute left-0" style="bottom: 2rem; width: max-content;">
          <div class="px-6 py-4 flex">
            <div class="ml-4">
              <p class="text-gray-500 text-sm">Food recommendations</p>
              <%= render "food_result_recommendation", nutri: nutri %>
            </div>
          </div>
        </div>
      </template>
    </div>
  </div>
<% end %>
